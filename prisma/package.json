{
  "name": "prisma",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "(export $(grep '^POSTGRES_URL' .env | xargs) && ts-node-dev --no-notify --respawn --transpileOnly ./src)",
    "seed": "(export $(grep '^POSTGRES_URL' .env | xargs) && ts-node prisma/seed.ts)",
    "postinstall": "prisma2 generate",
    "format": "prettier --write '**/*.ts' '**/*.json' && standard '**/*.ts' --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "3.0.6",
    "graphql-shield": "6.1.0",
    "graphql-yoga": "1.18.3",
    "jsonwebtoken": "8.5.1",
    "nexus": "0.12.0-beta.9"
  },
  "devDependencies": {
    "@types/bcrypt": "3.0.0",
    "@types/jsonwebtoken": "8.3.4",
    "@types/node": "^12.7.8",
    "prettier": "^1.18.2",
    "standard": "^14.3.1",
    "ts-node": "^8.4.1",
    "ts-node-dev": "1.0.0-pre.40",
    "typescript": "^3.6.3"
  }
}
